# Publish API key (required for PUT /api/summary from n8n or local curl)
PUBLISH_API_KEY=change-me-generate-with-openssl-rand-hex-32

# Production: auto-injected when you attach a Vercel Blob store to the project.
# Local dev: omit to use web/data/latest-summary.json instead.
# BLOB_READ_WRITE_TOKEN=
